'use client';

import { Box, Card, Heading, Text } from "@chakra-ui/react";

export default function Page() {'
    
    
    
    return (
        <Box>
            <Card>
                <Heading>Redirecting...</Heading>
                <Text>You should be redirected shortly to handle auth.</Text>
            </Card>
        </Box>
    );
}
